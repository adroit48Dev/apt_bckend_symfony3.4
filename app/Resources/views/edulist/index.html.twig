{% extends 'base.html.twig' %}

{% block content %}

<section id="page-title" class="padding-top-10 padding-bottom-15 padding-left-10" >
    <h2>Educational List</h2>
    <div class="container-fluid container-fullw bg-white">
        <!-- start: PAGE TITLE -->
        <div class="col-md-12">



            <div class="container-fluid container-fullw">
                <div class="row">
                    <div class="col-md-12">
                        <h5 class="over-title margin-bottom-15"> <span class="text-bold">Educational List</span></h5>
                        <div class="row">

                        </div>

                        <div class="table-responsive">
                            <table class="table table-striped table-hover" id="sample_2">




        <thead>
            <tr>
                <th>Id</th>
                <th>Edtitle</th>
                <th>Eddescription</th>
                <th>Edcreatedat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for eduList in eduLists %}
            <tr>
                <td><a href="{{ path('edulist_show', { 'id': eduList.id }) }}">{{ eduList.id }}</a></td>
                <td>{{ eduList.edTitle }}</td>
                <td>{{ eduList.edDescription }}</td>
                <td>{% if eduList.edCreatedAt %}{{ eduList.edCreatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('edulist_show', { 'id': eduList.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('edulist_edit', { 'id': eduList.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('edulist_new') }}">Create a new eduList</a>
        </li>
    </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
